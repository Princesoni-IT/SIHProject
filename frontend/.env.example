# Base URL of the backend API (provided by the backend team)
VITE_API_URL=http://localhost:5000/api

# When true, pages fall back to bundled sample data if an API call fails
# or while the backend isn't ready yet. Set to false for production.
VITE_DEMO_MODE=true
